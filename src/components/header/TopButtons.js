import React from 'react'

const TopButtons = () => {
  return (
    <div className='topButtons'>
        <button>Login</button>
        <button>Sign Up</button>
    </div>
  )
}

export default TopButtons